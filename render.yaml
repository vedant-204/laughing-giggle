services:
  - type: web
    name: proposal
    runtime: static
    buildCommand: echo "No build needed"
    staticPublishPath: .
    pullRequestPreviewsEnabled: true
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600

